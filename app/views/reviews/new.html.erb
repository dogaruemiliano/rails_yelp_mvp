<h1>New review</h1>
<%= simple_form_for [@restaurant, @review] do |form| %>
  <%= form.input :content %>
  <%= form.input :rating, collection: [1, 2, 3, 4, 5] %>
  <%= form.submit "Add a new review" %>
<% end %>
